#!/bin/bash

################################################################################
#
################################################################################

LOCATION="${HOME}/dev/Temperature/temperature"
days=7
T=$((60*24*${days}))
################################################################################

#find ${LOCATION} -type f \( -iname "*" ! -iname "*gz" \) -mmin +${T} -delete
find ${LOCATION} -type f \( -iname "*" \) -mmin +${T} -delete
##[ Fini ]######################################################################
